<div class="petition-container">
       <h1 class="title">Stand up against equality</h1>
    <div id="text-container">
        <h3>Half of the world's wealth is now in the hands of 1% of the population.<br>We strongly believe that this is unfair.<br>Sign the petition to give the 1% the other half as well.</h3>
    </div>
    <img id = "king" src="/public/images/theKing.jpg">
    <h2>{{deleteMessage}}</h2>
    <form action="/createNewSignature" method="POST">
      <div id="input-container">

            <input name="signature" type="hidden" id="hiddenInput">
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <canvas id="myCanvas"width="500" height="135"></canvas>

      </div>
           <button id="submitter" type="submit">Sign Up</button>
    </form>

    <div id = "typewrite-container">
        <h2 id="typewrite">Draw your signature down here and don't worry if it looks childish</h2>
    </div>
    <!-- <h1 class="error">{{error}}</h1> -->
</div>
